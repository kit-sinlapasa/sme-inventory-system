/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,jsx}'],
  theme: {
    extend: {
      colors: {
        // CR-010 (ขยายขอบเขต 2026-08-24) — ธีมของ**ทั้งระบบ** อิงสีจริงจาก
        // dashboard.render.com (ตรวจด้วย getComputedStyle จริง ไม่ใช่เดา) ซึ่งเป็นธีม
        // "สว่าง" ไม่ใช่ธีมมืดอย่างที่ตั้งใจไว้รอบแรกจากภาพตัวอย่างที่ไม่เกี่ยวกับ Render จริง
        ink: {
          bg: '#f9f9f7', // page plane
          surface: '#fcfcfb', // card surface (เกือบขาวแต่ไม่ใช่ขาวจ๋า ตาม Render)
          border: '#e5e7eb', // ตรงกับ rgb(229,231,235) ที่ Render ใช้จริง
          text: '#0b0b0b',
          muted: '#6b6b6b',
          // ทีมเลือกใช้สีฟ้าแทนม่วงของ Render (2026-08-24) — ใช้ค่าเดียวกับสีหมวดหมู่ RAM
          // ในกราฟ ทำให้ระบบมีสีฟ้าชุดเดียวทั้งธีมและกราฟ ไม่ใช่ฟ้าคนละเฉด
          // หมายเหตุ contrast: #2a78d6 บนพื้นการ์ด = 4.30:1 (ผ่าน WCAG AA สำหรับ UI component
          // และตัวอักษรใหญ่ แต่ต่ำกว่า 4.5:1 ของตัวอักษรปกติเล็กน้อย) ถ้าต้องการผ่าน AA เต็ม
          // เปลี่ยนเป็น #2168c4 ได้ (5.32:1) โดยไม่เปลี่ยนเฉดสี
          accent: '#2a78d6',
          accentSoft: '#eaf2fd', // ฟ้าอ่อนสำหรับพื้นไฮไลต์ (active nav, hover แถว)
        },
      },
    },
  },
  plugins: [],
}

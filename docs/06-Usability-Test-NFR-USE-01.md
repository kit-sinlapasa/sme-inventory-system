# NFR-USE-01 — Usability Test

> **เกณฑ์ที่ต้องพิสูจน์ (จาก [01-Requirements-Package.md](01-Requirements-Package.md)):**
> ผู้ใช้ใหม่ต้องทำ task ค้นหา/เช็คประกันสำเร็จ **≥ 90% ภายใน 60 วินาที โดยไม่ต้องมีคนสอน**
> · วิธีวัด: Task-based user test

---

## ⚠️ ขอบเขตของเอกสารนี้ — อ่านก่อน

เอกสารนี้แบ่งเป็น **2 ส่วนที่ห้ามสับสนกัน**

| ส่วน | ใครทำได้ | สถานะ |
|---|---|---|
| **ส่วน A** — สิ่งที่วัดได้โดยไม่ต้องมีผู้ใช้ (จำนวนขั้นตอน, heuristic evaluation, การเข้าถึงด้วยคีย์บอร์ด/โปรแกรมอ่านหน้าจอ, มือถือ) | ผู้พัฒนา/AI วัดได้ | ✅ ทำแล้ว — พบและแก้ข้อบกพร่อง **4 จุด** (D-01 ถึง D-04) |
| **ส่วน B** — ทดสอบกับผู้ใช้จริง | ต้องมีผู้ใช้จริงที่ไม่ได้พัฒนาระบบ | ✅ **ทำแล้ว 2026-08-25 — ผู้ที่ไม่ได้พัฒนาระบบ 7 คน สำเร็จ 7/7 เฉลี่ย 21.7 วินาที** (ทดสอบ 2 รอบ · รอบสองเพิ่มตามที่ผู้รีวิว PR ติง) · ตัดผู้พัฒนาออกจากกลุ่มตัวอย่าง · n=7 ยังไม่พอยืนยัน ≥90% เชิงสถิติ (ดู §B3, §B5) |

**ส่วน A ไม่ใช่ตัวแทนของส่วน B** — heuristic evaluation บอกได้ว่า "ตรงไหนน่าจะมีปัญหา" แต่บอกไม่ได้ว่า
"ผู้ใช้จริง 9 ใน 10 คนทำสำเร็จใน 60 วินาทีหรือไม่" ซึ่งเป็นสิ่งที่ NFR ข้อนี้ระบุไว้ตรง ๆ

ข้อมูลในส่วน B เป็นข้อมูลเชิงประจักษ์ **AI สร้างขึ้นเองไม่ได้อย่างซื่อสัตย์**
(บันทึกไว้ตั้งแต่ [02-AI-Usage-Log.md](02-AI-Usage-Log.md) entry #26 · AI ปฏิเสธการกรอกตัวเลขแทน 2 ครั้ง)
**ตัวเลขใน §B3 มาจากผู้ใช้จริงที่ทีมไปทดสอบเองเมื่อ 2026-08-25** ไม่ใช่ AI สร้างขึ้น

---

# ส่วน A — ผลที่วัดแล้ว (ไม่ต้องใช้ผู้ใช้)

## A1. เดิน task ตามเส้นทางจริงบน production

ทดสอบบน `https://sme-inventory-frontend.onrender.com` (2026-08-24)

| สิ่งที่วัด | ผล |
|---|---|
| องค์ประกอบที่กดได้บนหน้า | **2 ชิ้น** (ช่องกรอก + ปุ่ม) — ไม่มีสิ่งรบกวน |
| ขั้นตอนขั้นต่ำถึงผลลัพธ์ | **2 ขั้น** (พิมพ์ S/N → กดตรวจสอบ) |
| ต้องล็อกอินก่อนไหม | ไม่ต้อง |
| เวลาตอบกลับ (Render free tier, เครื่องตื่นแล้ว) | ~3 วินาที |
| ผลลัพธ์ที่แสดง | รุ่นสินค้า · สถานะประกัน · วันหมดประกัน (พ.ศ.) |

**สรุป A1:** โครงสร้าง task สั้นมาก (2 ขั้น) ซึ่งเอื้อต่อเป้าหมาย 60 วินาทีอย่างมาก
ข้อจำกัดหลักที่เหลือจึงไม่ใช่จำนวนขั้นตอน แต่คือ **ผู้ใช้จะรู้ไหมว่าต้องพิมพ์อะไร**

## A2. ข้อบกพร่องที่พบจากการเดิน task — และแก้แล้ว

### 🔴 D-01 ตัวอย่าง S/N บนหน้าจอ **ผิดรูปแบบ** (แก้แล้ว)

| | |
|---|---|
| **เดิม** | ช่องกรอกขึ้นตัวอย่าง `SN-0001234` |
| **รูปแบบจริงของระบบ** | `SN-<ประเภทสินค้า>-<เลข 5 หลัก>` เช่น `SN-PSU-01120` |
| **ต่างกันตรงไหน** | ตัวอย่างเดิม**ไม่มีส่วนประเภทสินค้า** และเลขยาว 7 หลักแทนที่จะเป็น 5 |
| **พิสูจน์** | ยิง `/api/public/warranty/SN-0001234` ได้ **404** — ตัวอย่างที่ระบบแนะนำเอง ค้นไม่มีวันเจอ |
| **ผลต่อ NFR** | ผู้ใช้ใหม่ที่ไม่มีคนสอนย่อมพิมพ์ตามรูปแบบบนหน้าจอ แล้วล้มเหลวตั้งแต่ครั้งแรก |
| **แก้เป็น** | `SN-GPU-00042` — รูปแบบถูกต้อง |

> **หมายเหตุที่พบระหว่างแก้:** ตอนแรกตั้งใจใส่ S/N ที่มีอยู่จริงในฐานข้อมูลเป็นตัวอย่าง
> แต่ตรวจแล้วพบว่า **S/N นั้นหายไปหลัง reseed** (เลข PSU ขยับจากช่วง 00xxx เป็น 00992–01265)
> การ hardcode S/N จริงจึงเป็นบั๊กเดิมในรูปแบบใหม่ — ตัวอย่างในช่องกรอกต้องเป็น
> **ตัวอย่างรูปแบบ** ที่ถูกต้องตามแพตเทิร์น ไม่ใช่ข้อมูลจริงที่ผูกกับชุดข้อมูลชุดใดชุดหนึ่ง

### 🔴 D-02 ข้อความ error เป็นทางตัน (แก้แล้ว)

| | |
|---|---|
| **เดิม** | "ไม่พบข้อมูล กรุณาตรวจสอบ S/N อีกครั้ง" |
| **ปัญหา** | ไม่บอกว่ารูปแบบที่ถูกต้องคืออะไร ผู้ใช้ที่พิมพ์ผิดรูปแบบจึงลองใหม่แบบเดิมซ้ำ ๆ |
| **รวมกับ D-01** | ผู้ใช้ทำตามตัวอย่าง → ผิด → error ไม่บอกทางออก → **ตันสนิท** ภายในไม่กี่วินาทีแรก |
| **แก้เป็น** | บอกรูปแบบที่ถูกต้องพร้อมตัวอย่างจริง + บอกกรณีที่ของยังไม่ถูกบันทึกขาย ให้ติดต่อสาขา |

> D-01 + D-02 รวมกันคือความเสี่ยงที่ตรงกับเกณฑ์ NFR มากที่สุด เพราะโจมตีเงื่อนไข
> **"โดยไม่ต้องมีคนสอน"** โดยตรง — ทั้งสองข้อแก้ก่อนรัน test กับผู้ใช้จริงแล้ว
> เพื่อไม่ให้ผลการทดสอบสะท้อนบั๊กที่รู้อยู่แล้วว่าต้องแก้

## A3. Heuristic evaluation (Nielsen 10) — เฉพาะหน้าเช็คประกัน

| # | Heuristic | ผล | หมายเหตุ |
|---|---|---|---|
| 1 | Visibility of system status | ✅ | ปุ่มเปลี่ยนเป็น "กำลังตรวจสอบ..." ระหว่างรอ |
| 2 | Match with real world | ✅ | ใช้คำว่า "หมายเลขซีเรียล (S/N)" + ระบุว่าหาได้จากสติกเกอร์/กล่อง · วันที่เป็น พ.ศ. |
| 3 | User control & freedom | ✅ | แก้ค่าแล้วค้นใหม่ได้ทันที ไม่มี state ค้าง |
| 4 | Consistency & standards | ✅ | ใช้ `.rd-*` ชุดเดียวกับทั้งระบบ |
| 5 | Error prevention | 🟡 | ยังไม่มี input mask/validate รูปแบบก่อนยิง API — ปล่อยให้ยิงแล้วค่อยบอกว่าไม่พบ |
| 6 | Recognition over recall | ✅ | ตัวอย่างรูปแบบอยู่ในช่องกรอก ไม่ต้องจำเอง (หลังแก้ D-01) |
| 7 | Flexibility & efficiency | ✅ | กด Enter ส่งฟอร์มได้ ไม่ต้องเอื้อมไปกดปุ่ม |
| 8 | Aesthetic & minimalist | ✅ | ทั้งหน้ามีองค์ประกอบกดได้ 2 ชิ้น |
| 9 | Help users recover from errors | ✅ | หลังแก้ D-02 — ก่อนหน้านี้ **ไม่ผ่าน** |
| 10 | Help & documentation | 🟡 | ไม่มีลิงก์ช่วยเหลือ/ตัวอย่างภาพว่าสติกเกอร์ S/N อยู่ตรงไหนของสินค้า |

**เหลือเป็นข้อเสนอ (ยังไม่ทำ):** #5 validate รูปแบบก่อนยิง API, #10 ใส่ภาพประกอบว่า S/N อยู่ตรงไหน
— ตั้งใจรอผลจากผู้ใช้จริงก่อนว่าจำเป็นจริงไหม **และผลออกมาแล้วใน §B3: ไม่มีใครทำไม่สำเร็จ
และไม่มีปัญหาใดพบซ้ำตั้งแต่ 2 คน** จึงยังไม่แก้ทั้งสองข้อ — เป็นการตัดสินใจจากข้อมูล ไม่ใช่การละเลย

## A4. วัดด้วยเครื่องมืออัตโนมัติบน production (`docs/screenshots/usability-checks.js`)

รันกับระบบที่ deploy จริง ไม่ใช่ dev server — **ผ่าน 8 จาก 10 ข้อ**

| หมวด | สิ่งที่วัด | ผล |
|---|---|---|
| ความเร็ว | ช่องกรอกพร้อมพิมพ์ | **0.6–1.0 วินาที** เมื่อเครื่องตื่นอยู่ (วัดซ้ำ 3 ครั้ง: 568 / 588 / 990 ms) · **~4.2 วินาที** ถ้าเป็น request แรกหลัง Render free tier หลับ — งบทั้ง task คือ 60 วินาที จึงยังเหลือเวลาเหลือเฟือทั้งสองกรณี |
| คีย์บอร์ด | กด Tab ครั้งแรกโฟกัสช่องกรอกเลย | ✅ ไม่ต้องไล่ Tab ผ่านอะไรก่อน |
| คีย์บอร์ด | กด Enter ส่งฟอร์มได้ | ✅ ทำ task ได้โดยไม่ต้องใช้เมาส์เลย |
| โปรแกรมอ่านหน้าจอ | มี `<h1>` บอกว่าหน้านี้คืออะไร | ✅ |
| โปรแกรมอ่านหน้าจอ | ปุ่มมีข้อความ ไม่ใช่ไอคอนเปล่า | ✅ |
| โปรแกรมอ่านหน้าจอ | ตั้ง `lang` ของหน้าเว็บ | ✅ `th` |
| มือถือ 375px | ไม่มีการเลื่อนแนวนอน | ✅ 375/375 px |
| มือถือ 375px | ทำ task สำเร็จ | ✅ |

### 🔴 D-03 ช่องกรอกไม่มีป้ายกำกับที่โปรแกรมอ่านหน้าจออ่านได้ (แก้แล้ว)

มีแค่ `placeholder` ซึ่ง **หายไปทันทีที่ผู้ใช้เริ่มพิมพ์** — คนที่ใช้โปรแกรมอ่านหน้าจอจึงไม่รู้ว่า
ช่องนี้ให้กรอกอะไร (ขัด WCAG 3.3.2 Labels or Instructions) · แก้โดยเพิ่ม
`aria-label="หมายเลขซีเรียล (S/N) ของสินค้า"`

### 🟡 D-04 ปุ่มบนมือถือสูง 38px (แก้เฉพาะหน้านี้)

**ผ่านเกณฑ์ AA อยู่แล้ว** (WCAG 2.5.8 Target Size Minimum = 24px) แต่ไม่ถึงเกณฑ์ AAA
(2.5.5 = 44px) · ขยายเป็น 44px เฉพาะหน้าเช็คประกัน เพราะเป็นหน้าเดียวที่ลูกค้าทั่วไป
ใช้จากมือถือหน้าร้าน และเป็น task ที่ NFR ข้อนี้วัดเวลาโดยตรง — ปุ่มในหลังบ้านที่พนักงาน
ใช้บนคอมพิวเตอร์คงไว้ที่ 38px ตามเดิม ไม่ขยายทั้งระบบเพื่อความสม่ำเสมอของหน้าตา

> **บั๊กในตัววัดเอง ที่ควรบันทึกไว้:** สคริปต์รอบแรกสลับลำดับพารามิเตอร์ของฟังก์ชันรายงานผล
> ทำให้ "ชื่อการทดสอบ" (string) ถูกใช้เป็นเงื่อนไข ซึ่ง truthy เสมอ → **ขึ้น ✅ ทั้ง 10 ข้อ
> ทั้งที่มี 2 ข้อที่ค่าจริงเป็น false** · จับได้เพราะคอลัมน์ชื่อพิมพ์ออกมาเป็น `true`/`false`
> แทนที่จะเป็นข้อความ — เป็นบั๊กชนิดเดียวกับ test ที่ fail ไม่ได้ (ดู AI Usage Log ข้อ 5
> ในหัวข้อ "สิ่งที่ AI ถูก Reject หรือแก้ไข") **เครื่องมือวัดที่บอกว่าผ่านหมด ควรถูกสงสัยก่อนเสมอ**

---

# ส่วน B — สคริปต์ทดสอบกับผู้ใช้จริง (ทีมต้องรันเอง)

## B1. เตรียมตัว

- **ผู้เข้าร่วม:** 5–8 คน ที่**ไม่ใช่คนทำระบบ** และไม่เคยเห็นระบบนี้มาก่อน
  (5 คนเพียงพอจะเจอปัญหาส่วนใหญ่ แต่ต้องอย่างน้อย **10 คนจึงจะอ้าง "90%" ได้อย่างมีความหมาย** —
  ถ้าได้ไม่ถึง 10 ให้รายงานเป็น "x/n คน" ตรง ๆ อย่าแปลงเป็นเปอร์เซ็นต์ให้ดูดี)
- **อุปกรณ์:** เครื่องของผู้เข้าร่วมเอง เปิด `https://sme-inventory-frontend.onrender.com`
- **สิ่งที่ต้องเตรียม:** S/N จริง 1 หมายเลขที่ขายแล้ว (ดูได้จากหน้าบันทึกขาย หรือถามผู้ดูแลระบบ)
- **จับเวลา:** เริ่มจับตอนพูดโจทย์จบ หยุดเมื่อผู้ใช้บอกคำตอบได้

## B2. บทพูด (อ่านตามนี้ ห้ามเพิ่มคำใบ้)

> "สมมติคุณซื้อการ์ดจอมาจากร้านนี้ แล้วอยากรู้ว่ายังอยู่ในประกันอยู่ไหม
> นี่คือหมายเลขซีเรียลบนตัวสินค้า: `____________`
> ช่วยหาคำตอบจากเว็บนี้ให้หน่อยครับ/ค่ะ — ระหว่างทำ ถ้าคิดอะไรอยู่พูดออกมาดัง ๆ ได้เลย"

**ถ้าผู้ใช้ถามคำถาม ให้ตอบว่า "ลองดูตามที่คิดได้เลยครับ/ค่ะ" เท่านั้น** — ห้ามชี้ทาง
เพราะเกณฑ์ระบุว่า "โดยไม่ต้องมีคนสอน" ถ้าสอนแล้วผลที่ได้จะไม่ตรงกับสิ่งที่ NFR ถาม

## B3. ผลการทดสอบจริง (2026-08-25)

ทดสอบ **2 รอบ** — รอบแรก 4 คน · รอบสองอีก 3 คนหลังผู้รีวิว PR ติงว่ากลุ่มตัวอย่างเล็กไป (ดู §B5)
รวมผู้ทดสอบ **7 คน** ที่ไม่ได้มีส่วนพัฒนาระบบ และไม่เคยเห็นระบบนี้มาก่อนวันทดสอบ

### ⚠️ ผู้ทดสอบไม่ได้เป็น "คนนอก" ทั้งหมด — ระบุให้ชัดตั้งแต่ต้น

| กลุ่ม | จำนวน | เป็นใคร |
|---|---|---|
| **คนนอกกลุ่มโครงงาน** | **2 คน** | นักศึกษาที่ไม่เกี่ยวข้องกับโครงงานนี้เลย |
| **สมาชิกกลุ่มที่ไม่ได้พัฒนาระบบ** | **5 คน** | รับผิดชอบ **รวบรวม requirement · วิเคราะห์และออกแบบระบบ · เอกสาร · นำเสนอ** — ไม่ได้เขียนโค้ดเลย และ**ไม่เคยใช้ระบบที่สร้างเสร็จแล้วก่อนวันทดสอบ** |
| **ผู้พัฒนาระบบ** | 1 คน | **ตัดออกจากผลแล้ว** (เดิมคือ P4) |

เอกสารฉบับก่อนเขียนว่า *"ผู้ที่ไม่ได้พัฒนาระบบ 7 คน"* ซึ่ง**ไม่ถูกต้อง** — แก้ไขเมื่อพบว่า 5 ใน 7
เป็นสมาชิกกลุ่มเดียวกัน · สองคำนี้มีน้ำหนักทางวิชาการต่างกัน จึงต้องเรียกให้ตรง

**ข้อควรระวังที่ต้องเปิดเผย:** สมาชิกกลุ่มทั้ง 5 คน**มีส่วนในการรวบรวม requirement และ
ออกแบบระบบ** จึงรู้ล่วงหน้าว่าหน้าจอนี้ควรทำอะไรได้ แม้จะไม่เคยใช้ของจริงมาก่อน —
นี่คืออคติที่เป็นไปได้และ**ไม่ปิดบัง**

**เหตุผลที่ยังใช้ข้อมูลได้:** ผู้พัฒนาเป็นผู้ commit ทั้งหมดเพียงคนเดียว (git บันทึก 91 commit
จากบัญชีเดียว) และที่สำคัญกว่านั้น — **ตัวเลขที่วัดได้ปฏิเสธอคติข้อนี้เอง** ถ้าความรู้จาก
การออกแบบช่วยให้ทำ task ได้เร็วขึ้นจริง กลุ่มนี้ต้องเร็วกว่าคนนอกกลุ่มอย่างเห็นได้ชัด
แต่ผลออกมาต่างกันเพียง 0.3 วินาที (ดูตารางถัดไป) · ต่างจากผู้พัฒนาที่เร็วกว่าคนอื่น 7 วินาที

**อธิบายได้ว่าทำไม:** การรู้ว่า *"ระบบควรเช็คประกันจาก S/N ได้"* ไม่ได้บอกว่า **ช่องกรอกอยู่ตรงไหน
บนหน้าจอ** ซึ่งเป็นสิ่งที่ usability test วัดจริง ๆ — คนที่เขียนโค้ดเองเท่านั้นที่รู้ข้อนั้น

### เปรียบเทียบสองกลุ่มย่อย — ไม่พบความแตกต่าง

| กลุ่ม | n | เฉลี่ย | ช่วง |
|---|---|---|---|
| คนนอกกลุ่ม | 2 | **21.5 วิ** | 17–26 |
| สมาชิกกลุ่มที่ไม่ได้พัฒนา | 5 | **21.8 วิ** | 18–30 |
| *(อ้างอิง)* ผู้พัฒนา | 1 | *10 วิ* | — |

**ต่างกันเพียง 0.3 วินาที** — การเป็นสมาชิกกลุ่มไม่ได้ทำให้ทำ task ได้เร็วขึ้น
ต่างจากผู้พัฒนาที่เร็วกว่าคนอื่นทุกคนอย่างชัดเจน (10 วิ เทียบกับคนที่เร็วรองลงมา 17 วิ)
เป็นหลักฐานเชิงประจักษ์ว่า**สิ่งที่ทำให้ได้เปรียบคือการสร้างระบบเอง ไม่ใช่การอยู่กลุ่มเดียวกัน**

> ⚠️ n=2 ในกลุ่มคนนอก **เล็กเกินกว่าจะสรุปเชิงสถิติ** ข้อสังเกตนี้จึงเป็นเพียงข้อบ่งชี้
> ไม่ใช่ข้อพิสูจน์ — ระบุไว้ตรง ๆ

### เงื่อนไขการทดสอบ

· ทดสอบทีละคน ไม่ให้ดูกัน
· แต่ละคนได้ S/N **คนละหมายเลข คนละรุ่น และคนละหมวดสินค้า** (GPU/Mainboard/CPU/RAM)
  เพื่อไม่ให้จำคำตอบจากคนก่อนหน้าได้ และได้ทดสอบไปด้วยว่าระบบตอบถูกทุกหมวด
· กติกาที่แจ้งผู้ดำเนินการก่อนเริ่ม: อ่านบทพูดตาม §B2 · ไม่ชี้ทาง · จับเวลาตั้งแต่พูดโจทย์จบ
  ถึงตอนผู้เข้าร่วมบอกคำตอบ · **บันทึกจุดที่ลังเลแม้ทำสำเร็จ**

> 🔒 บันทึกเป็นรหัส P1–P8 ไม่ใส่ชื่อ-รหัสนักศึกษาลงใน repo สาธารณะ
> ตามหลักเดียวกับ NFR-PRIV-01 · ผู้ดำเนินการเก็บว่าใครคือใครไว้ต่างหาก

| # | รอบ | ประเภทผู้ทดสอบ | S/N ที่ให้ (หมวด) | เวลา | ผล | สิ่งที่สังเกต |
|---|---|---|---|---|---|---|
| P1 | 1 | สมาชิกกลุ่ม | `SN-GPU-00780` (GPU) | 20 วิ | ✅ | ไม่ได้รายงานจุดสะดุด |
| P2 | 1 | สมาชิกกลุ่ม | `SN-MAINBOARD-00437` (Mainboard) | **30 วิ** | ✅ | ทำงานปกติ |
| P3 | 1 | สมาชิกกลุ่ม | `SN-MAINBOARD-00363` (Mainboard) | 20 วิ | ✅ | เร็ว ไม่ลังเล |
| P5 | 1 | สมาชิกกลุ่ม | `SN-CPU-00168` (CPU) | 18 วิ | ✅ | **งงหน้าจอตอนแรก** แล้วหาเจอเอง |
| P6 | 2 | **นอกกลุ่ม** | `SN-GPU-00810` (GPU) | 26 วิ | ✅ | ไม่ได้รายงานจุดสะดุด |
| P7 | 2 | **นอกกลุ่ม** | `SN-CPU-00210` (CPU) | **17 วิ** | ✅ | ไม่ได้รายงานจุดสะดุด |
| P8 | 2 | สมาชิกกลุ่ม | `SN-RAM-00005` (RAM) | 21 วิ | ✅ | ไม่ได้รายงานจุดสะดุด |

> ℹ️ **เลข P4 ถูกข้ามไปโดยตั้งใจ** — เดิมคือผู้พัฒนาระบบเอง ดูหัวข้อถัดไป
> คงเลขเดิมของคนอื่นไว้ไม่เลื่อน เพื่อให้อ้างอิงย้อนหลังตรงกัน

### ⚠️ ผู้พัฒนาไม่นับเป็นผู้เข้าร่วม

รอบแรกบันทึกไว้ 5 คน แต่ตรวจภายหลังพบว่า **P4 คือผู้พัฒนาระบบเอง** จึงตัดออก
เพราะละเมิดเงื่อนไขพื้นฐานที่ว่าผู้เข้าร่วมต้องไม่เคยเห็นระบบมาก่อน

**และเขาคือคนที่เร็วที่สุด (10 วินาที)** ซึ่งเป็นทิศทางของอคติที่คาดเดาได้พอดี
ถ้าปล่อยไว้จะดึงค่าเฉลี่ยให้ดูดีกว่าความจริง

**เก็บตัวเลข 10 วินาทีไว้เป็น baseline** — คือเวลาที่เร็วที่สุดเท่าที่เป็นไปได้จากคนที่รู้ทุกอย่าง
เกี่ยวกับหน้านี้ · **ผู้ทดสอบคนอื่นใช้เวลามากกว่า 1.7 – 3.0 เท่า**

### สรุปตัวเลข (ผู้ทดสอบ 7 คนที่ไม่ได้พัฒนาระบบ)

| | |
|---|---|
| สำเร็จภายใน 60 วินาที | **7 / 7 คน** |
| เวลาเฉลี่ย | **21.7 วินาที** |
| มัธยฐาน | 20 วินาที |
| เร็วสุด / ช้าสุด | 17 / **30** วินาที |
| คนที่ช้าที่สุดใช้งบเวลาไป | **50%** ของ 60 วินาที |
| *(อ้างอิง)* ผู้พัฒนาระบบ | *10 วินาที — ไม่นับรวม* |

**ทุกคนทำสำเร็จ และแม้แต่คนที่ช้าที่สุดก็ยังใช้เวลาเพียงครึ่งเดียวของงบ**
เวลาของทุกคนกระจุกอยู่ในช่วงแคบ (17–30 วิ) ไม่มีใครหลุดออกไปไกล บ่งว่าไม่มีกับดัก
ที่ทำให้บางคนติดนาน

### ⚠️ ข้อจำกัดที่ต้องระบุให้ชัด — ยังไม่ยืนยันเกณฑ์ ≥90% เชิงสถิติ

ตาม **rule of three** — เมื่อไม่พบความล้มเหลวเลยใน n ครั้ง ขอบบนของอัตราล้มเหลวจริงที่ระดับ
ความเชื่อมั่น 95% ประมาณ `3/n`

| | n | ขอบบนอัตราล้มเหลว 95% CI |
|---|---|---|
| รอบแรกก่อนตัดผู้พัฒนา *(ตัวเลขที่ผิด)* | 5 | 60% |
| รอบแรกหลังตัดผู้พัฒนา | 4 | 75% |
| **หลังทดสอบเพิ่มตามที่ผู้รีวิวติง** | **7** | **43%** |
| *ถ้านับเฉพาะคนนอกกลุ่ม* | *2* | *เกิน 100% — สรุปอะไรไม่ได้เลย* |
| ถ้าจะยืนยันเกณฑ์ได้จริง | ~30 | ~10% |

**จึงรายงานเป็น "7/7 คน" ไม่ใช่ "100%"** และไม่เคลมว่า NFR-USE-01 ผ่านเกณฑ์เชิงสถิติ
สิ่งที่พูดได้อย่างซื่อสัตย์คือ: *ทดสอบกับผู้ที่ไม่ได้พัฒนาระบบ 7 คน (ในจำนวนนี้เป็นคนนอกกลุ่ม 2 คน)
ทุกคนทำสำเร็จภายในครึ่งหนึ่งของเวลาที่กำหนด และไม่พบอุปสรรคที่ทำให้ใครทำไม่สำเร็จ*

### สิ่งที่สังเกตได้เชิงคุณภาพ

**P5 "งงหน้าจอตอนแรก"** — ยังคงเป็นข้อสังเกตเดียวจากผู้เข้าร่วมทั้ง 7 คน

ตามเกณฑ์ที่ตั้งไว้ก่อนเริ่มทดสอบ (**ปัญหาที่พบซ้ำตั้งแต่ 2 คนขึ้นไปจึงนับเป็นปัญหาจริง**)
ข้อนี้ **ไม่ถือเป็นปัญหาที่ยืนยันแล้ว** — และรอบสองที่เพิ่มอีก 3 คนก็**ไม่มีใครรายงานอาการ
เดียวกันซ้ำ** ทำให้เหลือ 1 ใน 7 · P5 ก็ไม่ใช่คนที่ช้าที่สุด (18 วิ เร็วกว่าค่าเฉลี่ย 21.7 วิ
และเร็วกว่า P2 ที่ 30 วิ กับ P6 ที่ 26 วิ) แสดงว่าความงงนั้นไม่ได้ทำให้เสียเวลาจริง

**ข้อจำกัดของการบันทึกรอบแรกที่ยอมรับตรง ๆ:** จดไว้แค่คำว่า "งง" ซึ่งไม่พอจะแก้อะไรได้
รอบสองจึงกำชับผู้ดำเนินการให้ถามต่อว่า *สะดุดตรงไหน* ถ้าใครลังเล — **แต่รอบสองไม่มีใครลังเล
จึงไม่ได้ข้อมูลเพิ่ม** · สรุปคือยังไม่รู้ว่า P5 งงตรงอะไร และ**ไม่แก้โค้ดจากข้อสังเกตเดียว
ที่ไม่รู้สาเหตุ** — ถ้าจะตามต่อ ต้องทดสอบเพิ่มแล้วเจาะตอนเกิดอาการ

> ⚠️ **บันทึกผลตามจริง ไม่ปรับให้สวย** — NFR-USE-01 เป็นระดับ *Should* ไม่ใช่ *Must*
> ผลที่ได้คือ 7/7 แต่เอกสารนี้เลือก**ไม่เคลมว่าผ่านเกณฑ์เชิงสถิติ** และระบุขอบบน
> ของอัตราล้มเหลวไว้ตรง ๆ ทุกรอบ — รวมถึงตอนที่การตัดผู้พัฒนาออกทำให้ตัวเลขแย่ลง
> การรายงานข้อจำกัดพร้อมเหตุผลมีค่าทางวิชาการมากกว่าการรายงานแต่ตัวเลขที่ดูดี


---

## B5. ข้อติงจากผู้รีวิว PR และการตอบสนอง

ผลรอบแรก (n=4) ถูกเสนอเป็น [PR #23](https://github.com/kit-sinlapasa/sme-inventory-system/pull/23)
และมีสมาชิกกลุ่มที่ไม่ใช่ผู้เขียนโค้ดรีวิว โดยตอบคำถามที่ตั้งไว้ในตัว PR ครบทั้ง 3 ข้อ
**ผู้รีวิวไม่ได้เห็นด้วยทุกข้อ** — บันทึกไว้ทั้งข้อที่รับและไม่รับ พร้อมเหตุผล

| # | ข้อติง | การตอบสนอง |
|---|---|---|
| 1 | **"อยากให้เก็บ [ผู้พัฒนา] ไว้แล้ว remark มากกว่า"** — ไม่เห็นด้วยกับการตัด P4 ออก | **รับบางส่วน** · ข้อมูล 10 วินาทีไม่ได้ถูกลบ — อยู่ในตารางเทียบและเก็บเป็น baseline ว่าผู้ใช้จริงช้ากว่าผู้พัฒนา 1.7–3.0 เท่า · แต่**ไม่นำกลับเข้าตัวเลขพาดหัว** เพราะตัวเลขพาดหัวตอบคำถามว่า *"คนที่ไม่เคยเห็นระบบใช้ได้ไหม"* ซึ่งคนเขียนระบบตอบแทนไม่ได้ |
| 2 | **"ใช้ชื่อกับนามสกุลเลยดีกว่า"** — แทนรหัส P1–P8 | **ไม่รับ** · repo เป็นสาธารณะ การใส่ชื่อ-นามสกุลผู้เข้าร่วมคือการเผยแพร่ข้อมูลส่วนบุคคลของคนอื่นโดยไม่ได้ขอความยินยอมให้เผยแพร่ต่อสาธารณะ · และจะย้อนแย้งกับ **NFR-PRIV-01** ที่ระบบนี้เขียนโค้ดบังคับใช้เอง (ซ่อนข้อมูลผู้ซื้อจาก API สาธารณะ) · ผู้ดำเนินการเก็บว่าใครคือใครไว้ต่างหาก ตรวจสอบย้อนหลังได้ถ้าอาจารย์ขอ |
| 3 | **"Ok แล้ว แต่อยากได้คน test เพิ่ม"** — ยืนยันว่าไม่มีเลขเก่าค้าง แต่กลุ่มตัวอย่างเล็กไป | **รับเต็ม ๆ และแก้แล้ว** · ทดสอบเพิ่มอีก **3 คน (P6–P8)** ทำให้ n เพิ่มจาก 4 → **7** และขอบบนอัตราล้มเหลวดีขึ้นจาก **75% → 43%** · ข้อติงนี้ตรงกับข้อจำกัดที่เอกสารยอมรับไว้เองอยู่แล้ว |

> ข้อ 3 คือเหตุผลที่มีรอบสอง — **การทดสอบเพิ่มเกิดขึ้นเพราะผู้รีวิวติง ไม่ใช่เพราะวางแผนไว้แต่แรก**
> ซึ่งเป็นตัวอย่างว่า code review ที่มีเนื้อหาจริงเปลี่ยนผลงานได้ ไม่ใช่แค่พิธีกรรม
